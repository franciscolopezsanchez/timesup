import React from "react"
import {connect} from "react-redux"

function TeamFinalScore() {
  return (
    <div>
      
    </div>
  )
}

const mapDispatchToProps = dispatch => ({
})

export default connect(null, mapDispatchToProps)(TeamFinalScore)
