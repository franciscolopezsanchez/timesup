import React from "react"
import { render, cleanup } from "@testing-library/react"
import SettingSelector from "./settingSelector"

afterEach(cleanup)

describe("SettingSelector tests", () => {
	test("displays a SettingSelector component", () => {
		// Arrange
		// Act
		// Assert
	})
})
